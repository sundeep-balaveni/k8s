
apiVersion: v1
kind: Pod
metadata:
  name: frontend
spec:
  containers:
  - name: app
    image: images.my-company.example/app:v4
    resources:
    #soft limit
      requests:
        memory: "64Mi"
        cpu: "250m"
    #hard limit
      limits:
        memory: "128Mi"
        cpu: "500m"